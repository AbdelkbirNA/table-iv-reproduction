"""Phase-1 entry point.

This script is intentionally a placeholder until the paper-specific faulty
implementations and generated test pools are available or a documented
simplified artifact-generation pipeline is selected.
"""


def main() -> None:
    raise SystemExit(
        "Phase 1 requires normalized HumanEval/GPT-5-mini fault + test-pool artifacts. "
        "See docs/experiment_plan.md."
    )


if __name__ == "__main__":
    main()
